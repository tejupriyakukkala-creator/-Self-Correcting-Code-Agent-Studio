[project]
name = "self-correcting-code-agent-studio"
version = "0.1.0"
description = "Self-Correcting Code Agent Studio"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "Flask>=2.0.0",
    "google-genai>=0.1.0",
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
    "requests>=2.25.0",
]

[tool.vercel]
entrypoint = "api/index:app"
